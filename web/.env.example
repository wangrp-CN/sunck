# 前端环境变量模板（构建时注入，打包进静态资源）
# 用法：cp web/.env.example web/.env && 填写 Key。web/.env 已被 .gitignore 忽略。

# API 基础路径：生产由 nginx 反代同源，填 /api 即可（无需写死域名）
VITE_API_BASE=/api

# 高德地图 Web 端 JS API Key（大屏地图 / 轨迹回放用，缺失时前端自动降级散点模式）
VITE_AMAP_KEY=
# 高德 JS API 2.0 安全密钥（必须同时配置，否则地图无法加载）
VITE_AMAP_SECURITY_CODE=
